# URL base de la API para `npm run dev`. Sin barra final.
# Déjala vacía sólo si algo sirve la API en el mismo origen que el frontend.
# En el despliegue con Docker no se usa: nginx hace de proxy de /api y la
# configuración se inyecta al arrancar el contenedor.
VITE_API_URL=http://localhost:8080
